import { Meta, Story, Preview, Props, ColorPalette, ColorItem } from '@storybook/addon-docs/blocks'
import * as Colors from './Colors'

<Meta title='Components|Colors' />

# Colors

```js
import { Colors } from @all-turtles/components

console.log(Colors.brand) // #D9534D
```

<ColorPalette>
  { Object.keys(Colors).map(key => <ColorItem title={key} colors={[Colors[key]]} />) }
</ColorPalette>
